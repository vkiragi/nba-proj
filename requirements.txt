# Runtime deps for Streamlit Community Cloud (which installs via pip, not uv).
# Mirrors the [project] deps + the `app` extra in pyproject.toml. The `explain`
# extra (shap/numba/llvmlite) is intentionally omitted — the app only *reads*
# the pre-generated SHAP image from docs/, it doesn't compute SHAP at runtime.
nba_api>=1.4
pandas>=2.2
pyarrow>=16
numpy>=1.26
scikit-learn>=1.5
xgboost>=2.1
matplotlib>=3.9
streamlit>=1.38
